# Add all components here using ADD_COMPONENT(<COMPONENT_DIRECTORY>)
ADD_COMPONENT(ConvertSegments)

ADD_COMPONENT(CspObjectRecognize)

ADD_COMPONENT(EdgeDetectorForSegmentedImage)

ADD_COMPONENT(GrayImageSegmentation)

ADD_COMPONENT(LineSegments)
